# Создание релизов

Workflow запускается при **push тега** (например, `v1.0.0`). Релиз создаётся автоматически — PAT не нужен.

## Как выпустить релиз

```bash
git tag v1.0.0
git push origin v1.0.0
```

Workflow соберёт образ и создаст релиз с описанием.
